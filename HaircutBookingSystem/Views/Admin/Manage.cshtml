@{
    ViewData["Title"] = "Admin Management";
}

<div class="auth-wrap">
    <div class="auth-card">
        <h1>Admin Management</h1>

        <div class="d-grid gap-2">
            <a class="btn btn-primary btn-cta" asp-action="AllRole">Manage Roles</a>
            <a class="btn btn-primary btn-cta" asp-controller="Account" asp-action="AllUser">Manage Users</a>
        </div>
    </div>
</div>
